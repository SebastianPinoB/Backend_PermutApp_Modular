# Librerias y configuracion

Cada microservicio mantiene su configuracion en `../Codigo fuente/<Servicio>/pom.xml`, `../Codigo fuente/<Servicio>/.env.example` y `../Codigo fuente/<Servicio>/src/main/resources/application.properties`.
